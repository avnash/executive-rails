<% content_for :title do%>
  <%= @tag.name %> articles
<% end %>
<% if signed_in? %>

  <%= render "shared/sidebar" %>
  <%= render "shared/right_sidebar" %>
<br>
<div class="post_main_container col-md-6 col-xs-8">
<%= render 'shared/secretary_profile' %>
<% if @posts.length!=0 %>
<% @user = @posts[0].user %>
<% end %>
<% @posts.each do |post| %>
  <%= render post %>
<% end %>

<% if @posts.length>10 %>
    <%= render 'shared/loadmore' %>
<% end %>

<% if @posts.length==0 %>

  <div class="post_div">No posts are active in this category.</div>
<% end %>
</div>
<% else %>

  <%= render 'shared/unsigned' %>

<%end%>
