<% content_for :title do%>
  Home
<% end %>

<% if signed_in? %>

  	<%= render "shared/sidebar" %>
  	<%= render "shared/right_sidebar" %>
<div class="post_main_container col-md-6 col-xs-12">
	<% @posts.each do |post| %>
  		<%= render post %>
	<% end %>

	<% if @posts.length>10 %>
    	<%= render 'shared/loadmore' %>
	<% end %>
</div>
<% else %>

  	<%= render 'shared/unsigned' %>

<% end %>
